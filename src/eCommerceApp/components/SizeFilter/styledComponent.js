import styled from '@emotion/styled';
import tw from 'tailwind.macro';

const DisplayBrandSize = styled.div `
  ${tw`
  w-1/4 px-4
  `}
`;

const Title = styled.p `
  ${tw`
  my-4 font-bold
  `}
`;

export {
  DisplayBrandSize,
  Title
}